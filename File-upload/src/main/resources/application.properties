spring.datasource.url=jdbc:mariadb://localhost:3306/FileUploads?useSSL=false
spring.datasource.username=bree
spring.datasource.password=bree004!!

# Remove explicit Hibernate dialect setting
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

spring.jpa.hibernate.ddl-auto=update

# Disable Open Session in View to suppress warning
spring.jpa.open-in-view=false

# Optionally, enable or disable SQL logging as needed
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

